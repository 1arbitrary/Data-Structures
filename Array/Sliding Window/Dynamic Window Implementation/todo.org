* Dynamic Sliding Window — Smallest Subarray ≥ S

** GOAL
I am trying to find the *smallest subarray length* whose sum is >= S.

---

** TODO Rep 1 — Add "Best So Far" Tracker
- [x] Declare a variable that stores the *minimum valid window length found so far*
- [x] Initialize it to a very large value (bigger than any possible window)

---

** TODO Rep 2 — Stop Tracking Window Size Manually
- [x] Remove any variable that increments/decrements window size
- [x] Do NOT store window size continuously
- [xx] Remember: window size = (right - left) only when needed

---

** TODO Rep 3 — Make Expansion Unconditional
- [x] In every outer loop iteration:
  - [x] Add arr[right] to the accumulator
  - [x] Move right forward
- [x] No condition should block expansion

---

** TODO Rep 4 — Detect Valid Window
- [x] Identify the condition that makes the window valid
- [x] This condition should control the shrinking loop

---

** TODO Rep 5 — Record Answer at the Right Time
Inside the shrinking loop:
- [] Before removing arr[left], compute current window size using (right - left)
- [] Compare it with the best-so-far minimum
- [] Store the smaller value

---

** TODO Rep 6 — Shrink Correctly
Still inside the shrinking loop:
- [x] Subtract arr[left] from accumulator
- [x] Move left forward
- [x] Continue shrinking while window remains valid

---

** TODO Rep 7 — Handle No-Solution Case
After the loop ends:
- [ ] If the minimum length was never updated, the answer should be 0

---

** TODO Rep 8 — Output the Final Answer
- [ ] Print the result so the program produces output

---

** Form Check (Read This Before You Run)
Your logic should read like this in English:

Expand window →
While window is valid:
  Record window size
  Shrink window
Repeat

If your logic says "if expand else shrink", that's incorrect.
